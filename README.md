# Livelylemur

A Python package for extracting text from images using OCR.

## Installation

### 1. Install System Dependencies

OpenCV requires certain system libraries to function properly:

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install -y libgl1-mesa-glx tesseract-ocr
